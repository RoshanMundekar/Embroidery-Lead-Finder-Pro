version: "3.8"

services:
  web:
    build: .
    container_name: embroidery-lead-finder
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
